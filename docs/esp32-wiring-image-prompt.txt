Create a highly accurate, crisp educational wiring infographic for an existing smart-pill-dispenser project. Landscape high resolution, white background, professional electronics manual, large readable labels, tasteful component illustrations. Title in Thai: "ผังการต่อ ESP32 — Smart Pill Dispenser". Subtitle: "ESP32 DevKit / ESP32-WROOM-32 • อ้างอิง config.h". Draw a LOGICAL CONNECTION DIAGRAM with a central ESP32 functional block (not a physical pin-position diagram), explicit port names, neatly routed colored wires and labelled net tags. Small caption "ผังเชิงตรรกะ • ตำแหน่งขาจริงดูสกรีนบนบอร์ด". Do NOT invent a physical pin order.

Exact electrical data:
Central ESP32 block has distinct named ports GPIO18, GPIO19, GPIO23, GPIO25, GPIO33, GPIO32, GPIO27, GPIO21 SDA, GPIO22 SCL, 3V3, GND, 5V/VIN.
RIGHT upper: Three individually illustrated servos labelled "Servo 1", "Servo 2", "Servo 3", each with SIG, V+, GND. GPIO18 goes ONLY to Servo1 SIG, GPIO19 ONLY Servo2 SIG, GPIO23 ONLY Servo3 SIG. Use orange distinct signal wires, labels GPIO18/19/23. Each V+ tagged "+5V BUS"; each GND tagged "GND". Caption "จ่ายไฟ Servo จากบัสโดยตรง • ตรวจว่ารองรับ 5V และสัญญาณ 3.3V".
LEFT upper: three normally-open pushbuttons each between its own GPIO and common GND: "Dispense — GPIO33", "Confirm — GPIO32", "Cancel — GPIO27". Show open-switch symbols; do not short to ground without switch. Caption "INPUT_PULLUP • กดแล้วต่อ GND".
RIGHT lower or another clear panel: GPIO25 wire to IN of a block explicitly labelled "Active buzzer + driver" with subtitle "เลือกโมดูล IN 3.3V / active HIGH", VCC "+5V BUS", GND "GND". This is an example compatible module choice because actual buzzer model is unknown. Do not depict a bare high-current buzzer wired directly to GPIO. Caption "วงจรตัวอย่าง • ตรวจรุ่น Buzzer".
LEFT lower: I2C path entirely explicit: ESP32 GPIO21 -> LV1 of "I²C bidirectional level shifter"; ESP32 GPIO22 -> LV2. ESP32 3V3 -> LV supply; +5V BUS -> HV supply; common GND -> GND. HV1 named SDA and HV2 named SCL fan out as a shared parallel bus to three blocks: "LCD 1 • เวลา • 0x27", "LCD 2 • ยา • 0x25", "RTC DS1307 • 0x68". Each module has SDA, SCL, VCC +5V BUS and GND net labels. Both LCDs are illustrated as small 16x2 screens, RTC as module with battery. SDA green and SCL blue never swapped. Caption "I²C 100 kHz • pull-up แต่ละฝั่งตามแรงดัน 3.3V / 5V". NO direct 5V I2C-to-ESP32 connection.
BOTTOM full-width power panel: "Adapter 5V regulated" positive -> "Fuse" -> "Switch" -> red "+5V BUS". Negative -> black "GND BUS". Capacitor "1000 µF" in PARALLEL across these rails with plus at +5V, minus at GND. Explicit net label list "+5V BUS → Servo 1–3 / LCD 1–2 / DS1307 / HV / Buzzer driver / ESP32 5V-VIN*" and "GND ร่วมกันทุกอุปกรณ์". Small note "*ตรวจสเปกขาไฟบอร์ดและการเสียบ USB ร่วมกับไฟภายนอก". Never connect servos to ESP32's 3V3 or route servo supply through board.
Separate dashed grey reserved box, with NO solid wires to sensors: "IR 1 / 2 / 3 — GPIO34 / 35 / 36 (สำรอง)" and "ยังไม่มีโค้ดอ่านค่า • ยืนยันรุ่นและระดับสัญญาณก่อนต่อ". These pins have no internal pullups; do not imply working sensors.
Two prominent small footer notes: "ห้ามต่อ 5V เข้า GPIO หรือ 3V3" and "ENABLE_SERVO_MOVEMENT = false • สอบเทียบก่อนเปิดใช้งาน".
Legend red=+5V, black=GND, orange=PWM/control, green=SDA, blue=SCL, purple=3.3V. Same named power net tags mean electrically common; label this briefly. Use clear component grouping and plentiful space, logical schematic lines rather than a breadboard photo or unverified PCB pinout. All connections and all above GPIO mappings must be exact. No Arduino Nano, no motor driver for servos, no WiFi/backend graphic. Prioritize readable Thai typography and unambiguous connections over decoration.
